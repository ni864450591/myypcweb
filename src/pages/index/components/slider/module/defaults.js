// 默认参数
const DEFAULTS = {
  // 初始索引
  initialIndex: 0,   //默认显示第0张
  // 切换时是否有动画
  animation: true,  
  // 切换速度，单位 ms
  speed: 300,
  // 自动切换，单位 ms
  autoplay: 0   //0不开启自动切换
};

export default DEFAULTS;
